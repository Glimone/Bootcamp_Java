public class Method {
    public static void main(String[] args) throws Exception {
        /*A estrutura de um método é a seguinte:
         
        type methodName = (adicione parâmetros se precisar) {...}
        
        Os métodos possuem alguns tipos de visibilidade em JAVA,eles são: Geral, herança, pacotes definidos ou classes.

        Existem situações com exceção dentro de um método, que não devem existir, então atenção nisso.

        Quando a classe não tiver retorno, deve-se usar a palavra-chave 'void'.
         
         */

        //Quando for se criar um método invisível para outras classes, deve-se usar o método em 'private'.
    }
}
